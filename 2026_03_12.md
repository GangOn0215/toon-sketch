# 📅 작업 로그 (2026-03-12)

## 📝 작업 목표 (Objectives)
- [x] 캐릭터 시트 하단 태그 삭제(X) 버튼 생성
- [x] 태그 재클릭 시 선택 해제(Deselect) 로직 구현
- [x] 일반화보 모드 이미지 비율(Aspect Ratio) 버그 수정
- [x] Next.js 16 Middleware → Proxy 전환 및 보안 문서 업데이트
- [x] SelectedTags UI 레이아웃 및 디자인 최적화
- [x] WorkspaceClient 하이드레이션 오류 최종 해결 (전역 CSS `[hidden]` 속성 제어)
- [x] GEMINI.md 지침 업데이트 (사용자 지시사항 상세 로그 기록 원칙 명시)

## 🕒 진행 상황 (Timeline)

### ✅ [Task 1~3 완료] - 캐릭터 시트 기능 및 UI 개선
- **개요**: 선택된 태그의 개별 삭제(X) 버튼 구현, 중복 클릭 시 선택 해제 로직 추가, 그리고 상단 여백 및 그림자 효과를 적용하여 UI를 세련되게 다듬었습니다.

### ✅ [Task 4 완료] - Next.js 16 Middleware → Proxy 전환
- **개요**: Next.js 16.1.6 버전의 미들웨어 지원 중단 경고를 해결하기 위해 `middleware.ts`를 `src/proxy.ts`로 통합/이전하고 관련 보안 문서를 업데이트했습니다.

### ✅ [Task 5~6 완료] - 하이드레이션 오류 최종 해결
- **개요**: 브라우저 확장 프로그램이 `MetadataWrapper` 내부의 `hidden` div에 `user-select` 스타일을 주입하면서 발생하는 하이드레이션 오류를 해결했습니다.
- **주요 조치**: 
    1. `globals.css`의 `[hidden]` 속성 요소에 `user-select: auto !important;` 전역 스타일 주입
    2. `layout.tsx`의 `body` 태그에 전역 스타일 설정
    3. `WorkspaceClient.tsx`에 Mounting 패턴 적용

### ✅ [Task 7 완료] - GEMINI.md 지침 업데이트
- **개요**: 사용자의 지시사항과 `todo.md`의 진행 상황을 로그에 상세히 기록해야 한다는 원칙을 `GEMINI.md`에 추가했습니다.

---

## 📋 2차 작업 세션 (오후)

### ✅ [Task 8] - 하이드레이션 오류 원인 규명 및 추가 수정
- **원인 확정**: `드래그프리` 브라우저 확장 프로그램이 모든 DOM 요소에 `user-select: auto` 스타일을 주입하는 것이 원인임을 확인
- **코드 수정**:
  - `ThemeToggle.tsx`: `suppressHydrationWarning` 추가 (mounted 전/후 렌더 불일치 방지)
  - `LoginSocial.tsx`: 버튼 색상 hex → rgb() 변환 (`#111` → `rgb(17,17,17)` 등 브라우저 정규화 불일치 해결)
  - `layout.tsx`: `<head>` 태그에 `suppressHydrationWarning` 추가
- **결론**: `user-select: auto` 관련 에러는 코드로 해결 불가, 확장 비활성화 또는 시크릿 모드 사용 권고

### ✅ [Task 9] - 로그인 페이지 레이아웃 개선 (Footer 추가 후 짜부 현상)
- **원인**: `nav`가 `position: fixed`라 플렉스 레이아웃에서 공간을 차지하지 않음. Footer 추가 후 `main`이 `100vh - footer높이`로 줄어드는 현상
- **수정**: 외부 div에 `paddingTop: "58px"` 추가, `main` padding 조정

### ✅ [Task 10] - 로그인 페이지 2컬럼 마케팅 카피 레이아웃
- **파일**: `src/app/login/page.tsx` 전면 개편
- **내용**:
  - 왼쪽: 마케팅 카피 영역 (배지, 헤드라인, 서브카피, 피처 태그, 소셜 프루프)
  - 오른쪽: 기존 로그인 카드
  - 헤드라인: "상상을 캐릭터로— / 지금 바로 / 시작하세요."
  - `min-height: calc(100vh - 58px)`으로 Footer와 함께 자연스럽게 배치
  - 모바일(820px 이하): 카피 숨기고 카드만 표시
  - `fadeUp` 애니메이션 (0.12s 딜레이 순차 등장)

### ✅ [Task 11] - CTA 섹션 Info 아이콘 위치 조정
- **파일**: `src/components/landing/CTASection.tsx`
- **내용**: Info 버튼을 inline → `position: absolute`, `left: calc(100% + 4px)`로 텍스트 오른쪽에 고정. padding 없이 absolute로만 위치 조정

### ✅ [Task 12] - 관리자 페이지 신규 생성 (`/admin`, `/admin/login`)
- **파일**:
  - `src/app/admin/page.tsx` (신규)
  - `src/app/admin/login/page.tsx` (신규)
- **디자인**: 초기 레트로 터미널 스타일 → 사용자 피드백으로 일반적인 SaaS 어드민 스타일로 변경
  - 다크 네이비 사이드바 (#1e293b) + 흰 콘텐츠 영역
  - Stat 카드 4개, 최근 가입자 테이블, 생성 성공/실패 바 차트
  - 한국어 메뉴 및 레이블
- **버그 수정**:
  - `<nav className="sb-nav">` → `<div>`로 교체: globals.css의 `nav { position: fixed }` 스타일이 admin 사이드바 내부 nav에도 적용되어 레이아웃 깨지는 현상 해결
  - 모바일 햄버거 버튼 + 드로어 슬라이드 추가
  - `topbar-time`의 `new Date()` SSR/CSR 불일치 → `useEffect + setInterval`로 클라이언트 전용 렌더링

### ✅ [Task 13] - 관리자 페이지 접근 권한 체크
- **파일**: `src/proxy.ts`, `src/app/admin/page.tsx`
- **접근 제어 흐름**:
  - 비로그인 → `/admin` 접근 시 `/admin/login` 리다이렉트
  - 일반 유저(role != 'admin') → `/admin` 접근 시 `/` 홈으로 리다이렉트
  - 이미 로그인한 관리자 → `/admin/login` 접근 시 `/admin` 자동 이동
  - 클라이언트 측 2차 권한 체크 (`useEffect`에서 role 확인)
- **Supabase 필요 작업**: `profiles` 테이블에 `role text DEFAULT 'user'` 컬럼 추가 및 관리자 계정에 `role = 'admin'` 설정

## 🔧 변경된 파일 목록 (2차 세션)
| 파일 | 작업 |
|------|------|
| `src/app/login/page.tsx` | 2컬럼 레이아웃 + 마케팅 카피 전면 개편 |
| `src/app/admin/page.tsx` | 신규 생성 (대시보드) |
| `src/app/admin/login/page.tsx` | 신규 생성 (관리자 로그인) |
| `src/components/ThemeToggle.tsx` | suppressHydrationWarning 추가 |
| `src/components/login/LoginSocial.tsx` | 색상 rgb() 변환 |
| `src/components/landing/CTASection.tsx` | Info 아이콘 absolute 위치 |
| `src/app/layout.tsx` | head suppressHydrationWarning |
| `src/proxy.ts` | 관리자 경로 가드 추가 |
| `src/app/globals.css` | cta-trust-row 스타일 조정 |

## 🚀 다음 계획 (Next Plan) — 2차 세션 기준
- Supabase `profiles` 테이블 `role` 컬럼 추가 및 관리자 계정 설정
- 관리자 페이지 각 메뉴 실제 기능 구현 (유저 관리, 결제 관리 등)
- 관리자 페이지 커밋 및 푸시

---

## 📋 3차 작업 세션 (오후 후반)

### ✅ [Task 14] - 관리자 Brute Force 공격 방어
- **파일**: `src/app/api/admin/login/route.ts` (신규)
- **내용**:
  - 관리자 로그인을 클라이언트 Supabase 직접 호출 → 서버 API 라우트(`/api/admin/login`)로 이관
  - Upstash Redis + `@upstash/ratelimit` 기반 IP당 슬라이딩 윈도우 rate limit 적용
  - **15분에 5회** 초과 시 429 차단 + Retry-After 헤더 반환
  - 로그인 실패 시 남은 시도 횟수 클라이언트에 표시
  - admin role 아닌 계정 로그인 시 즉시 세션 파기(signOut) 후 403 반환
  - 환경변수(`UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN`) 필요
- **admin/login/page.tsx 수정**: Supabase 직접 호출 제거, `/api/admin/login` fetch로 대체

### ✅ [Task 15] - 관리자 소셜 로그인 제외 확정
- 관리자 페이지에 Google/Kakao/Naver 소셜 로그인 미적용 결정
- **이유**: 외부 OAuth 계정 탈취 시 관리자 권한 동시 노출, 2FA 적용 불가, 계정 분리 원칙
- 이메일 + 비밀번호 전용 유지

### ✅ [Task 16] - 관리자 페이지 설계 문서 작성
- **파일**: `docs/admin-plan.md` (신규)
- **내용**: 라우트 구조, 구현 완료 항목, 메뉴별 상세 기능 계획, 권한 구조, 보안 원칙, Supabase 설정 SQL, 구현 우선순위

## 🔧 변경된 파일 목록 (3차 세션)
| 파일 | 작업 |
|------|------|
| `src/app/api/admin/login/route.ts` | 신규 생성 (Rate Limit + 로그인 처리) |
| `src/app/admin/login/page.tsx` | API 라우트 호출로 변경 |
| `docs/admin-plan.md` | 신규 생성 (관리자 설계 문서) |

## 🚀 다음 계획 (Next Plan) — 3차 세션 기준
- Supabase `profiles` 테이블 `role` 컬럼 추가 및 관리자 계정 설정
- 관리자 페이지 각 메뉴 실제 기능 구현 (유저 관리 → 결제 관리 순)
- 전체 커밋 및 푸시

---

## 📋 4차 작업 세션

### ✅ [Task 17] - 이메일 회원가입 기능 신규 생성
- **파일**:
  - `src/app/signup/page.tsx` (신규)
  - `src/components/signup/SignupEmail.tsx` (신규)
- **내용**:
  - 로그인 페이지와 동일한 2컬럼 레이아웃 (마케팅 카피 + 가입 폼)
  - 닉네임 / 이메일 / 비밀번호 / 비밀번호 확인 필드
  - 클라이언트 유효성 검사 (이메일 형식, 비밀번호 8자↑, 일치 여부)
  - Supabase `signUp()` 호출, `data.nickname` 메타데이터 저장
  - 가입 성공 시 이메일 인증 안내 화면 전환 (📬)
  - `emailRedirectTo`: `/auth/callback`
- **로그인 페이지 수정**: 소셜 로그인 카드 하단에 "이메일로 회원가입" 링크 추가

### 📌 [예정] - NICE 본인확인 API 연동
- 회원가입 플로우에 본인인증 단계 통합 예정
- 상세 계획은 `TODO.md` 참고

## 🔧 변경된 파일 목록 (4차 세션)
| 파일 | 작업 |
|------|------|
| `src/app/signup/page.tsx` | 신규 생성 (이메일 회원가입 페이지) |
| `src/components/signup/SignupEmail.tsx` | 신규 생성 (가입 폼 컴포넌트) |
| `src/app/login/page.tsx` | 회원가입 링크 추가 |
| `TODO.md` | 회원가입 / NICE 본인인증 / 관리자 항목 추가 |

## 🚀 다음 계획 (Next Plan) — 4차 세션 기준
- NICE 본인확인 API 연동 (환경변수: `NICE_CLIENT_ID`, `NICE_CLIENT_SECRET`)
- Supabase `profiles` 테이블 `role` 컬럼 추가 및 관리자 계정 설정
- 관리자 페이지 각 메뉴 실제 기능 구현

---

## 📋 5차 작업 세션

### ❌ [검토] - NICE 본인확인 API 보류
- **이유**: 연간 50~220만원, 사업자 계약 필수, 개발 전 테스트 환경 없음
- **대안 검토**: Twilio ($0.0494/건, 한국 번호 불가) → 탈락
- **최종 결정**: CoolSMS(SOLAPI) 채택 — 건당 18원, 한국 번호 발신, 즉시 사용 가능

### ✅ [Task 18] - 회원가입 SMS OTP 인증 구현 (CoolSMS)
- **플로우 변경**: NICE 팝업 → 3단계 SMS OTP 방식으로 전환
  1. 닉네임/이메일/비밀번호 입력
  2. 휴대폰 번호 입력 → CoolSMS SMS 발송
  3. OTP 6자리 입력 → 검증 → Supabase signUp
- **신규 파일**:
  - `src/app/api/auth/phone/send/route.ts`
    - CoolSMS HMAC-SHA256 인증 방식
    - Redis OTP 저장 (3분 TTL)
    - 재전송 쿨다운 60초 (Redis)
    - `profiles.phone` 중복 번호 사전 차단
  - `src/app/api/auth/phone/verify/route.ts`
    - Redis OTP 대조
    - 5회 초과 차단
    - 인증 성공 시 verifiedToken 발급 (5분 TTL)
  - `src/lib/nice.ts` — NICE 공통 유틸 (보류 보존)
  - `src/app/api/auth/nice/` — NICE API 라우트 (보류 보존)
  - `src/app/auth/nice/result/` — NICE 팝업 결과 페이지 (보류 보존)
  - `src/components/signup/NiceVerification.tsx` (보류 보존)
- **수정 파일**:
  - `src/app/signup/page.tsx`: 3단계 플로우 + 진행 StepIndicator UI
  - `src/components/signup/SignupEmail.tsx`: 버튼 텍스트 "다음 — 휴대폰 인증"
  - `src/components/signup/SignupEmail.tsx`: 비밀번호 아이콘 lucide Eye/EyeOff 교체

## 🔧 변경된 파일 목록 (5차 세션)
| 파일 | 작업 |
|------|------|
| `src/app/api/auth/phone/send/route.ts` | 신규 (CoolSMS OTP 발송) |
| `src/app/api/auth/phone/verify/route.ts` | 신규 (OTP 검증) |
| `src/lib/nice.ts` | 신규 (NICE 유틸, 보류) |
| `src/app/api/auth/nice/` | 신규 (NICE API, 보류) |
| `src/app/auth/nice/result/page.tsx` | 신규 (NICE 팝업 결과, 보류) |
| `src/components/signup/NiceVerification.tsx` | 신규 (NICE 컴포넌트, 보류) |
| `src/app/signup/page.tsx` | 3단계 OTP 플로우로 전면 수정 |
| `src/components/signup/SignupEmail.tsx` | 버튼 텍스트 + Eye 아이콘 교체 |

## 🚀 다음 계획 (Next Plan) — 5차 세션 기준
- CoolSMS 환경변수 설정 (`COOLSMS_API_KEY`, `COOLSMS_API_SECRET`, `COOLSMS_FROM_NUMBER`)
- Supabase `profiles` 테이블 `phone` 컬럼 추가 (`UNIQUE`)
- Supabase `profiles` 테이블 `role` 컬럼 추가 + 관리자 계정 설정
- 관리자 페이지 각 메뉴 실제 기능 구현

---

## 📋 6차 작업 세션

### ✅ [Task 19] - SMS OTP 보안 강화
- **일일 발송 횟수 제한** (`/api/auth/phone/send`)
  - 번호당 하루 10회 초과 시 차단 (Redis, 24시간 TTL)
  - 기존 60초 쿨다운과 2중 방어
- **휴대폰 번호 유효성 검증 강화**
  - 서버: `010` 정확히 11자리 / `011/016/017/018/019` 10~11자리 정규식 검증
  - 클라이언트: 버튼 클릭 전 사전 차단 + 실시간 빨간 테두리 피드백
- **자동 하이픈 포맷** (`signup/page.tsx`)
  - 타이핑 시 `010-XXXX-XXXX` 형식 자동 적용, 11자리 초과 입력 차단
- **비밀번호 아이콘** — lucide `Eye`/`EyeOff` 교체 (이모지 제거)

### 🔧 [이슈] - SOLAPI 발신번호 오류 (3059)
- **증상**: SMS 발송 시 `변작된 발신번호` 오류
- **원인**: SOLAPI 대시보드 발신번호 미승인 또는 번호 형식 오류
- **해결 방법**: SOLAPI → 발신번호 관리 → 승인 상태 확인 / `.env` 하이픈 없이 입력

## 🔧 변경된 파일 목록 (6차 세션)
| 파일 | 작업 |
|------|------|
| `src/app/api/auth/phone/send/route.ts` | 일일 발송 10회 제한, 번호 정규식 강화 |
| `src/app/signup/page.tsx` | 자동 하이픈, 실시간 유효성 피드백, 클라이언트 검증 |
| `src/components/signup/SignupEmail.tsx` | Eye/EyeOff 아이콘 교체 |

## 🚀 다음 계획 (Next Plan) — 6차 세션 기준
- SOLAPI 발신번호 3059 오류 해결 후 SMS 테스트
- Supabase `profiles` 테이블 `phone UNIQUE` 컬럼 추가
- Supabase `profiles` 테이블 `role` 컬럼 추가 + 관리자 계정 설정
- 전체 커밋 및 푸시

---

## 📋 7차 작업 세션

### ✅ [Task 20] - 로그인 페이지 이메일/비밀번호 로그인 추가
- **파일**: `src/components/login/LoginEmail.tsx` (신규)
  - 이메일 + 비밀번호 입력 폼
  - Eye/EyeOff 아이콘 (lucide)
  - 이메일/비밀번호 미입력 시 버튼 비활성화
  - Enter 키 로그인 지원
- **`login/page.tsx`**: `handleEmailLogin` 핸들러 추가
  - `supabase.auth.signInWithPassword()` 호출
  - 에러 한국어 처리 (잘못된 자격증명, 이메일 미인증)
- **`LoginSocial.tsx`** 레이아웃 개편
  - 이메일 폼 → 회원가입 링크 → 소셜 버튼 순으로 재배치
  - 소셜 버튼: 세로 큰 버튼 → 48×48 원형 아이콘 3개 (가운데 정렬)
  - 회원가입 링크 LoginSocial 내부로 이동 (login/page.tsx에서 제거)

## 🔧 변경된 파일 목록 (7차 세션)
| 파일 | 작업 |
|------|------|
| `src/components/login/LoginEmail.tsx` | 신규 생성 (이메일 로그인 폼) |
| `src/components/login/LoginSocial.tsx` | 레이아웃 재배치 + 소셜 원형 아이콘 |
| `src/app/login/page.tsx` | handleEmailLogin 추가, 회원가입 링크 정리 |

## 🚀 다음 계획 (Next Plan) — 7차 세션 기준
- SOLAPI 발신번호 3059 오류 해결 후 SMS 테스트
- Supabase `profiles` 테이블 `phone UNIQUE` 컬럼 추가
- Supabase `profiles` 테이블 `role` 컬럼 추가 + 관리자 계정 설정
